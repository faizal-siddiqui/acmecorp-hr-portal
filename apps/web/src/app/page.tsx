export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8">
      <div className="text-center space-y-2">
        <h1 className="text-3xl font-bold tracking-tight">Salary Management</h1>
        <p className="text-muted-foreground text-sm">
          ACME Corp HR — employee directory, compensation &amp; analytics
        </p>
      </div>
      <p className="rounded border px-4 py-2 text-sm font-mono text-muted-foreground">
        🚧 Skeleton — building in progress (Story A1)
      </p>
    </main>
  );
}
